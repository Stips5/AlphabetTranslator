<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="hr.stips.alphabettranslator">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/apk_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PrevoditeljActivity"
                  android:label="Prevoditelj"
                  android:theme="@style/AppTheme">

        </activity>


        <activity android:name=".VjezbaPrevodenjaActivity"
                  android:label="Vjezba prevođenja"
                  android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".PostavkeActivity"
                  android:label="Postavke"
                  android:theme="@style/AppTheme">

        </activity>

        <activity android:name=".AboutActivity"
                  android:label="About"
                  android:theme="@style/AppTheme">

        </activity>

    </application>

</manifest>